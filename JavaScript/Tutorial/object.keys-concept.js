let sonu={
    rollNo:38,
    course:"B.tech",
    branch:"CSBS",
}
for(let i=0; i<Object.keys(sonu).length; i++){
    console.log("The " + Object.keys(sonu)[i] + " of sonu is " + sonu[Object.keys(sonu)[i]])
}
//For in loop repalce this concept 