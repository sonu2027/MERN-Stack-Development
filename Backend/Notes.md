/ => Home route
client --> req
server <--res

--> Type of req
   get, post etc

1) npm init => install a application from npm
init is a utility like create react app, vite etc
npm init create package.json file

2) Edit package.json scripts like :
 "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"npm index.js"
  },

3) Create index.js file
4) Run this file => npm run start
5) install express => npm i express --save